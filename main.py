from tkinter import *

window = Tk() # instatiate an instance of a window
window.geometry("690x690")
window.title("Fox's firt window")
window.icon

window.mainloop() # shows window on screen, listens for events
